(()=>{
  if(window.__JMW_DASHBOARD_PATCH__)return;window.__JMW_DASHBOARD_PATCH__=true;
  const labels={received:'รับเรื่องแล้ว',checking:'กำลังตรวจสอบ',waiting_parts:'รออะไหล่',repairing:'กำลังซ่อม',completed:'ซ่อมเสร็จแล้ว',cancelled:'ยกเลิก'};
  const $=id=>document.getElementById(id);
  const dateKey=v=>String(v??'').slice(0,10);
  const todayKey=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
  const draw=(id,type,labels,data)=>{const el=$(id);if(!el||!window.Chart)return;if(window[id+'x'])window[id+'x'].destroy();window[id+'x']=new Chart(el,{type,data:{labels,datasets:[{data,borderWidth:1}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:type==='bar'?'bottom':'right'}}}})};
  async function refresh(){
    const [dr,ur,rr]=await Promise.all([fetch('/api/devices',{cache:'no-store',headers:{Authorization:`Bearer ${localStorage.getItem('jmw_token')}`}}),fetch('/api/users',{cache:'no-store',headers:{Authorization:`Bearer ${localStorage.getItem('jmw_token')}`}}),fetch('/api/repairs',{cache:'no-store',headers:{Authorization:`Bearer ${localStorage.getItem('jmw_token')}`}})]);
    if(!dr.ok||!ur.ok||!rr.ok)return;
    const devices=await dr.json(),users=await ur.json(),repairs=await rr.json();
    const activeUsers=(Array.isArray(users)?users:[]).filter(u=>u.active!==false);
    const deviceList=Array.isArray(devices)?devices:[];
    const assignedIds=new Set(activeUsers.filter(u=>u.device_id).map(u=>Number(u.device_id)));
    const repairCount=deviceList.filter(d=>d.status==='repair').length;
    const retiredCount=deviceList.filter(d=>d.status==='retired').length;
    const assignedCount=deviceList.filter(d=>assignedIds.has(Number(d.id))&&d.status!=='repair'&&d.status!=='retired').length;
    const availableCount=Math.max(0,deviceList.length-assignedCount-repairCount-retiredCount);
    const overdueCount=activeUsers.filter(u=>dateKey(u.return_due_date)&&dateKey(u.return_due_date)<todayKey()).length;
    const repairList=Array.isArray(repairs)?repairs:[];
    const openRepairs=repairList.filter(r=>!['completed','cancelled'].includes(r.status)).length;
    if($('total'))$('total').textContent=deviceList.length;
    if($('available'))$('available').textContent=availableCount;
    if($('assigned'))$('assigned').textContent=assignedCount;
    if($('repair'))$('repair').textContent=repairCount;
    if($('overdue'))$('overdue').textContent=overdueCount;
    if($('sumUsers'))$('sumUsers').textContent=activeUsers.length;
    if($('sumRepairs'))$('sumRepairs').textContent=repairList.length;
    if($('sumOpenRepairs'))$('sumOpenRepairs').textContent=openRepairs;
    if($('sumRetired'))$('sumRetired').textContent=retiredCount;
    draw('deviceChart','doughnut',['พร้อมใช้งาน','กำลังใช้งาน','กำลังซ่อม','เลิกใช้'],[availableCount,assignedCount,repairCount,retiredCount]);
    const order=['received','checking','waiting_parts','repairing','completed','cancelled'];
    const rc={};order.forEach(k=>rc[k]=0);repairList.forEach(r=>{if(Object.prototype.hasOwnProperty.call(rc,r.status))rc[r.status]++;});
    const present=order.filter(k=>rc[k]>0);
    draw('repairChart','doughnut',present.length?present.map(k=>labels[k]||k):['ยังไม่มีข้อมูล'],present.length?present.map(k=>rc[k]):[0]);
    const userCounts={};activeUsers.forEach(u=>{const k=u.class_or_department||'ไม่ระบุ';userCounts[k]=(userCounts[k]||0)+1});const ukeys=Object.keys(userCounts);draw('userChart','bar',ukeys.length?ukeys:['ยังไม่มีข้อมูล'],ukeys.length?ukeys.map(k=>userCounts[k]):[0]);
    const due={overdue:0,within30:0,within90:0,later:0};const today=new Date(`${todayKey()}T00:00:00`);activeUsers.forEach(u=>{const key=dateKey(u.return_due_date);if(!key)return;const d=new Date(`${key}T00:00:00`),diff=Math.floor((d-today)/86400000);if(diff<0)due.overdue++;else if(diff<=30)due.within30++;else if(diff<=90)due.within90++;else due.later++});draw('dueChart','bar',['เกินกำหนด','ภายใน 30 วัน','ภายใน 90 วัน','มากกว่า 90 วัน'],[due.overdue,due.within30,due.within90,due.later]);
  }
  const start=()=>setTimeout(()=>refresh().catch(e=>console.warn('[JMW DASHBOARD]',e)),0);
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
  window.jmwDashboardRefresh=refresh;
})();